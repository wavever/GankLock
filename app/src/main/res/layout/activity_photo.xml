<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <ImageView
        android:id="@+id/photo_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:src="@drawable/gank2"/>

    <LinearLayout
        android:id="@+id/photo_bottom_option"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="bottom"
        android:paddingBottom="@dimen/photo_activity_option_padding_bottom"
        android:gravity="center_horizontal">

        <TextView
            style="@style/PhotoActivityBottomOption"
            android:id="@+id/photo_share"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableTop="@drawable/ic_settings_white_36dp"
            android:text="分享"/>

        <TextView
            style="@style/PhotoActivityBottomOption"
            android:id="@+id/photo_wallpaper"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableTop="@drawable/ic_settings_white_36dp"
            android:text="设为壁纸"/>

        <TextView
            style="@style/PhotoActivityBottomOption"
            android:id="@+id/photo_lock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableTop="@drawable/ic_settings_white_36dp"
            android:text="设为锁屏"/>
    </LinearLayout>

    <TextView
        android:id="@+id/photo_save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:textColor="@color/white"
        android:clickable="true"
        android:textSize="@dimen/photo_activity_save_text_size"
        android:layout_marginRight="@dimen/photo_activity_save_margin_right"
        android:layout_marginBottom="@dimen/photo_activity_save_margin_bottom"
        android:text="保存"/>
</FrameLayout>